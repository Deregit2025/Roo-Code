# .orchestration/active_intents.yaml
active_intent: REQ-1

intents:
  - id: REQ-1
    description: "Implement reasoning loop initialization"
    scope:
      - "src/hooks/*"
      - "src/intent/*"
    constraints:
      max_runtime: 60
      requires_approval: true

  - id: REQ-2
    description: "Add code scaffolding for new agent tools"
    scope:
      - "src/tools/*"
      - "src/orchestration/*"
    constraints:
      max_runtime: 120
      requires_approval: false
